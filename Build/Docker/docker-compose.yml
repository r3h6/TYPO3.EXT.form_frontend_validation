version: '2.3'
services:

  composer:
    image: typo3gmbh/${DOCKER_PHP_IMAGE:?err}:latest
    user: ${HOST_UID:?err}
    volumes:
    - ${ROOT_DIR:?err}:${ROOT_DIR:?err}
    - /etc/passwd:/etc/passwd:ro
    - /etc/group:/etc/group:ro
    working_dir: ${ROOT_DIR:?err}
    entrypoint: composer
